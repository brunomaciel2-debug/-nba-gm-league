name: Resolve FA Offers

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  resolve:
    runs-on: ubuntu-latest
    steps:
      - name: Call resolve-offers endpoint
        run: |
          curl -X GET https://nba-gm-league-mu.vercel.app/api/cron/resolve-offers
